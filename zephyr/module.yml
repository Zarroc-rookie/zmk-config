# Zephyr module manifest for Verdant02 user-config.
# Field names are SINGULAR (board_root / shield_root), verified against
# working upstream zmk-config templates. board_root also triggers shield
# discovery in the same tree's `boards/shields/<name>/`.

name: zmk-keyboards-verdant02

build:
  settings:
    board_root: config
