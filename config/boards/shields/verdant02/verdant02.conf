# Safe minimal ZMK 4.x config. Only enable USB + BLE explicitly;
# debounce/logging/idle all inherit ZMK/app defaults to avoid any
# Kconfig unknown-symbol warnings that -Werror-abort the build.
CONFIG_ZMK_USB=y
CONFIG_ZMK_BLE=y
